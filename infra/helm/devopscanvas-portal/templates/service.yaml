apiVersion: v1
kind: Service
metadata:
  name: {{ include "devopscanvas-portal.fullname" . }}
  labels:
    {{- include "devopscanvas-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devopscanvas-portal.selectorLabels" . | nindent 4 }}